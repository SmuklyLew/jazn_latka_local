# v14.8.5.032 archive_workspace_runtime_sqlite test import hotfix

## Problem

GitHub Actions failed during pytest collection:

```text
ModuleNotFoundError: No module named 'archive_workspace_runtime_sqlite'
```

`tests/test_archive_workspace_runtime_sqlite.py` imported `archive_workspace_runtime_sqlite` as a top-level module, but the implementation file lives at `docs/history/archive_workspace_runtime_sqlite.py`.

## Fix

The test now loads the historical script by explicit file path using `importlib.util.spec_from_file_location`, inserts the module into `sys.modules` before execution, and then binds `apply_archive` and `preflight` from that loaded module.

This keeps the historical script in `docs/history/` and avoids pretending it is an installed top-level package.

## Validation

Run locally in the unpacked runtime:

```bash
python -m py_compile tests/test_archive_workspace_runtime_sqlite.py docs/history/archive_workspace_runtime_sqlite.py
pytest -q tests/test_archive_workspace_runtime_sqlite.py
```

Result:

```text
4 passed in 0.16s
```

## Patch SHA256

```text
64d5b1401d10f70ebe882fb9c71059049bb1f29d86aae70c2d2f5dea2590f618
```
