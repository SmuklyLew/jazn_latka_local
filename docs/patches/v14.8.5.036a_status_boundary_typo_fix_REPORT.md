# v14.8.5.036a — status boundary typo fix

## Cel

Mikro-patch porządkujący opisowe pola `truth_boundary` w statusach runtime/daemonu po v14.8.5.036.

## Zakres

- wersja pakietu: `v14.8.5.036a`,
- release name: `time-source-status-boundary-typo-fix`,
- regresyjny test spacji w polskich opisach statusu,
- brak zmiany mechaniki daemonu, czasu, pamięci ani truth gate.

## Granica prawdy

Ten patch dotyczy tekstu diagnostycznego/statusowego. Nie zmienia kontraktu uruchomienia runtime: brak trusted network time nadal nie blokuje startu, a lokalny fallback czasu pozostaje jawnie niezweryfikowany.

## Testy zalecane

```powershell
python -X utf8 -m compileall -q main.py latka_jazn tests
pytest -q tests/test_v1485036a_status_boundary_typos.py tests/test_v1485015_runtime_access_contract.py
python -X utf8 main.py --daemon-stop
python -X utf8 main.py --daemon-start
python -X utf8 main.py --daemon-status
python -X utf8 main.py --network-time-check
```
